101-4	PREFIX text: <http://jena.apache.org/text#> prefix skos: <http://www.w3.org/2004/02/skos/core#> PREFIX mv:<http://purl.obolibrary.org/obo/TEMP#> prefix dcterms:<http://purl.org/dc/terms/> SELECT DISTINCT ?docs WHERE { ?docs mv:subject/skos:prefLabel ?subject . { skos:animacion skos:prefLabel ?prefLabel . FILTER regex(?subject, '.*'+str(?prefLabel)+'.*') . } UNION {FILTER regex(?subject, '.*vision.*computador.*') .} }